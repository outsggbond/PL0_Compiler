const a := 10;   const a = 10
var   b, c d;  var   b, c, d;
//单行注释
/*
* 多行注释
*/
procedure procedure fun1;   procedure fun1;
    if a <= 10 then
        begin
            c = b + a   c:= b + a
        end;
begin
    read(b;   read(b);
    while b # 0 do
        begin
            call fun1;
            write 2 * c);   write (2 * c);
            read(b);
        end
end.
